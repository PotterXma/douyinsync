# `_bmad/` — BMAD 模块配置（本仓库）

- **`bmm/config.yaml`**：项目名、语言、`_bmad-output` 子路径、`project_knowledge` 等。
- **`bmm/user-config.yaml`**：可选个人覆盖（勿提交密钥）。
- **`_config/bmad-help.csv`**：轻量「目录索引」行，供需要 `bmad-help.csv` 的技能做元数据路由。

完整规划正文在 **`../_bmad-output/`**，勿与本目录混淆。
